# 3.13.19

Fixed
- A message sent into another session from an agent could land in that
  session's input box without being sent, so the other side never saw it. Short
  commands were fine; longer messages were the ones that quietly went missing.
  The Return now always registers, and the reply says whether it was pressed.
