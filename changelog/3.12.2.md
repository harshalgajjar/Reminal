# 3.12.2

Fixed
- On Windows, your coding agents report their state again. The command reminal
  set up for them had a Windows path the agent's own shell mangled — it silently
  failed, so no working / needs-you / done ever came through. It's written in a
  form the shell accepts now. Re-run `reminal integrate` on Windows to pick it up.
