# 3.13.6

Improved
- If reminal updates itself while an AI agent is connected through it, the agent
  is now warned that the set of reminal actions it first loaded may be out of
  date — so it refreshes instead of trusting a stale list. Before this, an agent
  could quietly keep working from the older version and conclude a feature was
  missing when it had actually already shipped.
