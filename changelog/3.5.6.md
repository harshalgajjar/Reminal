# 3.5.6

- The closed-lid display census is the daemon's job now instead of every
  session's — measured at ~30 AppleScript spawns a minute on a laptop with
  twelve sessions open, all but one redundant.
- Battery readings expire after a week, so a laptop shut in a drawer stops
  claiming a charge.

Fixes
- A lock-liveness probe read EPERM as dead and could have grown a second
  virtual display.
- A cached battery value was handed out by pointer.
