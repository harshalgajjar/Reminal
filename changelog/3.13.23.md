# 3.13.23

Fixed
- An agent could freeze after reminal set it up to report its state. The small
  callback reminal installs waited for the agent to finish handing over its
  event, and if the agent kept that channel open it waited forever — stalling
  the agent itself, since it runs these callbacks and waits for them. The
  callback now gives up on its own and never makes an agent wait.
