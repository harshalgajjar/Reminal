# 3.13.11

Fixed
- Uploading several files at once now puts a space between the paths it types
  into your shell. They used to run together into one unusable word, so you had
  to separate them by hand before the command would work.
