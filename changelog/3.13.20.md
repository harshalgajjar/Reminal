# 3.13.20

Added
- An exposed port in the web machines list is now something you can open. Tap
  it and the forwarded app appears in a new tab, already past its PIN prompt —
  no copying the link and no typing the PIN. Only on machines you own, and the
  PIN is fetched for that one tap rather than kept in the list.
