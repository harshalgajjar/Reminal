# 3.14.14

Fixed
- A Claude Code session in a long, quiet tool call no longer shows as done
  on your machines list. Claude Code can report a turn as finished the
  moment its words end, before a tool it called has completed; reminal now
  reads Claude Code's own "esc to interrupt" footer as the sign of a
  running turn.
